---
title: Welcome to the Switch to Salesforce blog
description: How we use MDX, Shiki, and Giscus — with categories, tags, and a sample Apex-style block.
date: "2026-04-04"
category: Getting started
tags:
  - salesforce
  - mdx
  - nextjs
coverImage: ""
author:
  name: Rajneesh Pal
  bio: Salesforce-focused engineer writing clear guides for developers and admins making the switch to the platform.
  github: https://github.com/
  linkedin: https://www.linkedin.com/
  trailhead: https://trailhead.salesforce.com/
---

This post lives in `content/blog/` and is loaded through the **`lib/content.ts`** service layer so you can swap in an API later.

<Callout type="info">
  **Phase 2** adds Shiki via `rehype-pretty-code`, Giscus comments, richer metadata, and a `public/search.json` index script.
</Callout>

## Fenced code (Shiki + one-dark-pro in dark mode)

```ts
type Trail = { id: string; name: string };

const t: Trail = { id: "1", name: "Admin" };
console.log(t.name);
```

```bash
npm run dev
```

```json
{ "theme": "one-dark-pro" }
```

Apex-style fences map to **Java** highlighting for now:

```apex
public class Hello {
  public static void run() {
    System.debug('Hello');
  }
}
```

## Custom components

<CodeBlock language="npm">
npm install remark-gfm
</CodeBlock>

Thanks for reading — add more `.mdx` files under `content/blog/`.
