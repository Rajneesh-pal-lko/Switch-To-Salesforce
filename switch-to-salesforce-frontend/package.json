{
  "name": "switch-to-salesforce-frontend",
  "version": "1.0.1",
  "private": true,
  "description": "Static frontend — build step only writes API URL for production",
  "scripts": {
    "build": "node scripts/write-api-config.js",
    "start": "python3 -m http.server 5500"
  },
  "engines": {
    "node": ">=18"
  }
}
